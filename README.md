# Basket

This project now targets the modern Android toolchain and uses the Gradle 9.0 milestone wrapper. Sync and builds have been verified with the Android Gradle Plugin 8.6.1 and the Material 1.12.0 libraries. Use a JDK that is compatible with Gradle 9 (JDK 17–21) when running the Gradle wrapper locally.
